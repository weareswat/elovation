<tr>
  <td>
    <%= image_tag(gravatar_url(table_rating, :size => 32)) %>
    <%= link_to table_rating.name, player_game_path(table_rating.player_id, @game.id) %>
  </td>
  <td><strong><%= table_rating.value %></strong></td>
  <td><%= table_rating.total_points %></td>
  <td><%= table_rating.avg %></td>
  <td><%= table_rating.total_games %></td>
  <td><%= table_rating.victories %></td>
  <td><%= table_rating.losses %></td>
</tr>
